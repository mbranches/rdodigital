package com.branches.atividade.repository;

import com.branches.atividade.domain.FotoDeAtividadeEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FotoDeAtividadeRepository extends JpaRepository<FotoDeAtividadeEntity, Long> {
}